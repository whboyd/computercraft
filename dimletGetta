i = peripheral.wrap("left")

items = i.getAvailableItems()

--for k, item in pairs(items) do
  --print("dimlet " .. item.fingerprint.dmg .. " amount " .. item.size)
--  deets = i.getItemDetail(item.fingerprint)
--end

print(textutils.serialize(items[2]))
deets = i.getItemDetail(items[2].fingerprint)
print(deets)
